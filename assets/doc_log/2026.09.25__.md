I'll build this as a Claude Code project scaffold you can drop into an empty repo: `CLAUDE.md`, skills, subagent, MCP config, and docs that answer the design questions up front. First I'm setting up the task list and finding my working directory.Working directory confirmed and folders created. Task 1 is next, the governance files. I'm writing the discovery transcript verbatim and tagging every decision as stated, inferred or assumed.Task 1 is nearly done. Next I'm writing the remaining governance docs and the scheduler spec, with test numbers computed against a fixture travel matrix.The BLAQTAXXI scaffold is built and zipped: 28 files, with no app code yet. Unzip it into an empty repo root, hidden `.claude/` and `.mcp.json` included.

Claude Code will do the rest from the docs: `CLAUDE.md` for the rules, and `docs/DECISIONS.md` for the answered questions. Every decision is tagged stated, inferred or assumed, so Claude shouldn't ask you anything that's already answered.

- **Scheduler spec:** `docs/SCHEDULER_SPEC.md` covers your Lewisville → Downtown rush-hour case, the return-leg case and the "right around the corner" case. I recomputed every number in it from the fixture with a script, and all of them matched. The fixture travel times are illustrative, not measured traffic.
- **Build plan:** `docs/BUILD_PLAN.md` has 9 phases and 13 episodes. Each has a prompt you can paste, acceptance checks and a review gate.
- **Skills and reviewers:** five skills cover the scheduler, live tracking, n8n, demo-mode fallbacks and episode capture. Two read-only reviewer agents check the scheduler and the pre-deploy state.
- **MCP servers:** `.mcp.json` lists seven servers. I couldn't launch any of them from here, so run `/mcp` first.
- **Skills index:** Claude Code reads skills from `.claude/skills/*/SKILL.md`. The root `SKILLS.md` is only an index for your students.

Things I inferred or assumed that you should check:
- **Lewisville:** I read "Louisville, Lil' M … $25" as Lewisville → Dallas at $25.
- **Build number:** 032, from your `projects.csv`, and the repo name and subdomain follow your existing pattern.
- **Brand:** the app uses your IAS colors as a demo skin. Your palette has no warning or error color, so late-ride states use an icon and text until you approve one (D-73).
- **Cancellation and no-show policy:** 50% retained inside 60 minutes, and a 5-minute no-show grace. These are my guesses. They live in one config file.
- **Midnight rule:** a ride must finish by 24:00. This is also a guess.
- **Price tiers:** $20 up to 30 minutes, $25 for 30–45, $30 above 45. Only the $20 and $25–30 figures came from your notes.
- **Legal:** insurance, permits and privacy are unverified in `docs/COMPLIANCE_CHECKLIST.md`. It's a list of questions, not legal advice.

The six items still open for you are at the bottom of `DECISIONS.md`, and none of them blocks Phases 0–2. Start with the Episode 0 prompt in `docs/BUILD_PLAN.md`.