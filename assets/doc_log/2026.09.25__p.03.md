# Concept Draft (p2)
PROMPT  
---  
1. Yes, Lewisville. As in Lewisville, TX
2. This is build 32
3. For Cancellation and no-show policy we should follow an average of Uber and Lfyt. Customers understand this from using these apps before using Blaqtaxxi.
4. "Midnight Rule" = Whatever the driver has set as his ending time (in this case midnight) means the last ride must be booked by this time. No rides can be booked after the drivers cut off time. An edge case.. . next week he can't be a taxi due to his Daughters soccer game so on Thurday his schedule ends at 2pm. All rides for that day my be booked by 2pm. 
5. The price tiers are correct for the MVP


To clarify the first prompt (and the mode).
In plan mode give the following prompt from the root:
"Read `CLAUDE.md`, then `docs/DECISIONS.md`, `docs/PRD.md`..."

Also to clarify about the MCP
When you say "run /mcp" what do you mean?
---  

## Response  

Before I update the docs, I'm looking up Uber's and Lyft's current cancellation and no-show rules. I don't want to write "the average of Uber and Lyft" from memory, because those policies change and vary by city.The searches only returned links, so I'm reading the official help pages directly.I could read Lyft's pages but not Uber's, which returned 404s. So I'll only mark what I verified as verified. I'm updating the docs with your answers now.Cutoff and policy changes are in. Now I'm making the plan-mode start and the `/mcp` explanation explicit in the docs, and adding the cutoff rule to the scheduling skill.Your answers are applied and the scaffold is re-zipped, replacing the earlier one.

**Plan mode and the first prompt.** From the repo root, start Claude Code with `claude --permission-mode plan`. If you're already in a session, press `Shift+Tab` until the indicator under the prompt box says plan mode. Then paste your "Read `CLAUDE.md`, then `docs/DECISIONS.md`, `docs/PRD.md`…" prompt. I added a fourth ask, "your plan for Phase 0", so plan mode has something to end on. Approving that plan is the "go" that starts Phase 0. The updated Episode 0 section in `docs/BUILD_PLAN.md` and the README both say this.

**What `/mcp` means.** It's a slash command you type inside a running Claude Code session, at its prompt box, not in your terminal shell. It lists the servers from `.mcp.json`, shows which are connected, and lets you sign in to the ones that use a browser login (Neon and Vercel). From a normal terminal, `claude mcp list` shows the same servers. I couldn't launch any of the seven from here, so this check is the first thing to do.

**Your answers:**
1. **Lewisville, TX** is confirmed and logged.
2. **Build 032** is confirmed.
3. **Cancellation policy** now follows Uber and Lyft. I could read Lyft's help pages but not Uber's, which returned 404s.
   - **Verified for Lyft:** a fee applies if you cancel within 1 hour of a scheduled pickup with a driver matched.
   - **Verified for Lyft:** a no-show needs the driver to arrive, wait, and try to contact you.
   - **Not published by Lyft or Uber:** fixed dollar amounts and the wait length. Lyft says fees vary with demand, so I had nothing to average.
   - **My picks:** free cancel until T-60 min, a $5 fee inside 60 minutes, and a $10 fee once the driver is heading to you or you no-show. The no-show wait is 5 minutes. A rider can also cancel free if the driver is projected 10 or more minutes late.
   - **To change these:** it's one config file (`policy.config.ts`), so you or the driver should decide the amounts.
4. **The midnight rule.** Availability end time is now a cutoff on when a ride can start, and a ride may finish after it. Hours can be overridden per date, including closing a day, so a Thursday can end at 2pm. Rides already booked past a shortened day are kept and flagged, never auto-cancelled. I added test cases for all of this (S5b–c, S11a–c).
5. **Price tiers** are confirmed for the MVP.

Please confirm my reading of the cutoff: that "booked by 2pm" means the last pickup can be scheduled at 2pm. It could also mean the rider has to book before 2pm, which would be a different rule.

Still open: whether this is a real launch or a course demo, the cancellation dollar amounts, the SMS provider, and a warning color for late-ride states.