---
description: Record a new decision or assumption in docs/DECISIONS.md (usage /log-decision <text>)
argument-hint: <decision text>
---

Add this to `docs/DECISIONS.md` in the right section with the next free id, a tag (`stated`, `inferred`, or `assumed`), a confidence (H/M/L) and a "where it lives" pointer: **$ARGUMENTS**

If it contradicts an existing decision, show me both and ask which stands before editing. If it should be a config value, tell me which config file it belongs in.
